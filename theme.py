"""GoodNightBot — общая pixel-art тема (ночь/кладбище, в тон игре) + цвета грейдов.

Палитра спокойная, многослойная: тёмная база → поверхности-карточки → мягкие бордеры,
ОДИН тёплый акцент (лунное золото) + сдержанные статусы. Без неона — чтобы не рябило
и читалось. Имена сохранены (control.py/db_browser ссылаются), значения приглушены.
"""

# ─── ночная база (тёмный индиго, меньше фиолета) ───
NIGHT   = "#14111e"   # фон окна (глубокая ночь)
PANEL   = "#1b1830"   # поверхность/карточка
PANEL2  = "#231f3c"   # карточка светлее / hover
CARD    = PANEL        # семантический алиас
SURFACE = PANEL2
EDGE    = "#2e2747"   # мягкий бордер/разделитель
EDGE_HI = "#473c6b"   # активный бордер
BORDER  = EDGE

# ─── текст: чёткая иерархия (3 уровня) ───
INK     = "#e7e4f1"   # основной (не чисто-белый — мягче)
SUB     = "#8e87ac"   # вторичный
FAINT   = "#615a80"   # тусклый/подписи
TEXT     = INK
TEXT_DIM = SUB
TEXT_MUTE = FAINT

# ─── акцент: лунное золото (единственный «тёплый» цвет) ───
MOON    = "#e6cf86"   # луна/золото (приглушено)
MOONDIM = "#b9a866"
STAR    = "#f3ead0"

# ─── статусы (сдержанные, не неон) ───
GO      = "#46b27c"   # СТАРТ (спокойный зелёный)
GO_HI   = "#54c98e"   # hover
GO_INK  = "#06241a"   # текст на зелёном
STOPC   = "#d6645c"   # СТОП/опасное (приглушённый красный)
STOP_INK= "#2a0d0c"
WARN    = "#d9b25c"   # предупреждение (тёплый янтарь)

# ─── цвета грейдов (10 тиров) — для подсветки логов «в тон грейда» (чуть приглушены) ───
GRADE = {
    "обычный":      "#c4c4d2",   # 0 белый
    "необычный":    "#5fc06a",   # 1 зелёный
    "редкий":       "#5a9ef0",   # 2 синий
    "легендарный":  "#e6a347",   # 3 оранж/золото
    "бессмертный":  "#e85a5a",   # 4 красный
    "аркана":       "#a86ae6",   # 5 фиолет
    "запредельный": "#e87cd0",   # 6 розовый
    "celestial":    "#5fd9cf",   # 7 бирюза
    "божественный": "#e6c860",   # 8 золото
    "космический":  "#e86aa8",   # 9 пурпур
}
# англ-синонимы классификатора рамки -> русский грейд
GRADE_EN = {
    "common": "обычный", "uncommon": "необычный", "rare": "редкий",
    "legendary": "легендарный", "epic": "аркана", "red": "бессмертный",
}

# ─── событийные цвета логов ───
EV = {
    "merge":  MOON,      # мерж
    "mail":   "#5a9ef0", # почта
    "chest":  "#e6a347", # сундуки
    "save":   SUB,       # раскладка
    "idle":   FAINT,     # ждёт
    "ok":     GO,
    "warn":   WARN,
    "err":    STOPC,
}

# моноширинный «пиксельный» шрифт: что найдётся в системе (по приоритету)
PIX_FONTS = ("Consolas", "Lucida Console", "Courier New", "Courier")
